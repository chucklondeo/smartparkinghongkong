<!--
Template for the Draft PR described in docs/website-rebuild-plan.md §12.
Fill in every [ ] and [TODO] once the actual code changes exist — this file
is written in advance so the PR can be opened quickly once Git/GitHub write
access is available. Delete this comment block before pasting into `gh pr
create --body-file` or the GitHub UI.
-->

## Goal

Rebuild the Londeo Access marketing site from a single scrolling page into a
real, independently-crawlable, bilingual (EN / ZH-HK) site: dedicated product
and solution pages, working navigation and footer links, a hardened lead
form, and baseline SEO (per-page metadata, canonical, hreflang, robots,
sitemap, structured data). Full rationale and grounding in
`docs/site-audit-baseline.md`, `docs/website-rebuild-plan.md`,
`docs/content-claims-review.md`, `docs/content-required.md`.

## Page structure changes

- [TODO] list of routes added, e.g. `/platform`, `/products`, `/products/*` (4),
  `/solutions`, `/solutions/*` (5), `/case-studies`, `/about`, `/contact`,
  `/privacy-policy`, `/terms-of-service`, `/thank-you`, and their `/zh-hk/*`
  mirrors.
- [TODO] confirm `/` (homepage) still resolves and its canonical is unchanged.

## New pages

- [TODO] confirm count and list against `website-rebuild-plan.md` §2.1.

## Product display changes

- [TODO] product cards (image/name/model/one-liner/3 specs/View
  Details/Download Datasheet/Request a Quote) — list which of the 4 products
  have real content vs. `Content required` state, per `content-required.md`.

## Form & conversion changes

- [TODO] describe Supabase Edge Function added (honeypot/rate limit/email
  notification), required-field logic (`Email` **or** `WhatsApp`), UTM/source
  capture, `/thank-you` redirect.
- [TODO] confirm WhatsApp/email links are real and functional (pending real
  WhatsApp number per `content-required.md` §7).

## SEO changes

- [TODO] per-route metadata/canonical/hreflang, `app/robots.ts`,
  `app/sitemap.ts`, JSON-LD (Organization/Product/BreadcrumbList/FAQ),
  `.htaccess` 404 fix.

## Test results

- [TODO] paste `npm run lint`, `npx tsc --noEmit`, `npm run build` output
  (must be run in a normal environment — the Cowork sandbox used for
  planning could not complete `build`/`lint` due to a network restriction on
  `next/font/google`; see `site-audit-baseline.md` §3).
- [TODO] manual test checklist results from `website-rebuild-plan.md` §13.

## Still needs company confirmation

- Everything listed in `docs/content-required.md` (product photos, specs,
  certifications, case studies, legal entity/address, real WhatsApp number).
- Every claim in `docs/content-claims-review.md` (usage stats, "ISO 27001",
  "certified" language, "all major brands," "4–6 weeks," etc.) — each needs
  evidence, an approved neutral rewrite, or explicit "Demo Data" labeling
  before this PR is considered content-complete.
- Confirm current production location of "North Territory" text (not found
  in this repository — see `site-audit-baseline.md` §7).

## Screenshots / preview

[TODO] attach desktop (1440px) and mobile (375px) screenshots of key pages,
or a preview deployment URL if one is generated by CI for this branch.

## Rollback

- This PR targets `claude/londeo-site-optimization-20260728`, branched from
  `main` at the same commit as `backup/pre-londeo-site-optimization-20260728`.
- Revert = close this PR without merging, or `git revert` the merge commit if
  it was already merged. `main` and the `deploy` branch (Hostinger's live
  source) are untouched by this branch until a human explicitly merges.
- No database migrations are included; the Supabase Edge Function addition is
  additive and can be disabled/removed independently of the frontend changes.
